<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gray_600</item>
        <item name="colorPrimaryVariant">@color/gray_800</item>
        <item name="colorOnPrimary">@color/pastel_orange_300</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/pastel_orange_400</item>
        <item name="colorSecondaryVariant">@color/pastel_orange_800</item>
        <item name="colorOnSecondary">@color/gray_800</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Font. -->
        <item name="android:fontFamily">@font/raleway</item>
    </style>

    <style name="Theme.AppTheme.Launcher">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>